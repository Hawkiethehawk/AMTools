# AMTools 工作规则

## 仓库边界

- 当前 Git 仓库是 AMTools 唯一的开发、运行和发布来源。
- 所有路径按仓库根目录动态解析，不依赖固定盘符、用户名或其他工作区。
- AMDC 和 AMDA 的旧源码归档不得作为隐式回退来源。

## 项目边界

- `apps/AMDC` 负责 AppMagic 数据采集、账号认证、缓存、历史、Excel、飞书表格同步和本地看板。
- `skills/AMDA` 负责已批准数据的统计分析、市场洞察、图表和飞书报告 Demo/正式文档流程。
- `packages/contracts` 只放跨模块数据契约，不放账号、令牌、资源地址或报告正文。
- `orchestrator` 只负责流程编排和状态传递，不复制 AMDC 或 AMDA 的业务计算。

## 安全与授权

- 真实采集、飞书同步、正式文档覆盖、账号备份和远程推送必须由用户明确授权。
- 默认只运行语法检查、契约测试、健康检查和隔离验证。
- 不得提交 `amdc-config.json`、账号登录态、令牌、云资源地址、缓存、日志和运行产物。
- 账号备份目标必须是私有仓库，并从本机私有配置读取。
- AMDA 分析必须绑定明确的 `CollectionManifest` 或不可变数据快照。

## 变更规则

- 不改变 AMDC 的账号分工、采集周顺序、并发方式和进度口径，除非先获得专项确认。
- 不改变 AMDA 的报告结构、图表契约和 Demo/正式文档确认边界，除非先获得专项确认。
- 修改共享契约时，必须同步更新契约校验和两端适配说明。
