# AMTools 工作规则

默认使用中文回答。

## 规则入口与仓库边界

- 本文件是 AMTools 唯一的项目规则入口，子目录不再维护独立 `AGENTS.md`。
- 当前 Git 仓库是 AMTools 唯一的开发、运行和发布来源；仓库根目录以 `git rev-parse --show-toplevel` 的结果为准。
- 所有路径按仓库根目录动态解析，不依赖固定盘符、用户名或其他工作区。
- AMDC 和 AMDA 的旧源码归档只作历史查询，不得作为运行时来源、隐式回退、开发分支或第三份长期维护副本。
- 不得把 AMTools 纳入其他仓库，也不得使用其他仓库的提交或发布脚本操作 AMTools 远端。

## 项目边界

- `apps/AMDC` 负责 AppMagic 数据采集、账号认证、缓存、历史记录、Excel、飞书表格同步和本地看板。
- `skills/AMDA` 负责已批准数据的统计分析、市场洞察、图表和飞书报告 Demo/正式文档流程。
- AMDA 图表产物只允许写入 `skills/AMDA/output/charts`，不得写入仓库根目录或其他临时产物目录。
- `packages/contracts` 只存放跨模块数据契约，不存放账号、令牌、资源地址或报告正文。
- `orchestrator` 只负责流程校验、编排和状态传递，不复制 AMDC 或 AMDA 的业务计算。

## 执行授权

- 用户明确说明动作、对象和范围的直接执行指令即为授权；仅要求评估、审查、说明或给出方案不代表授权实施。
- 未获得直接执行授权时，只进行必要的只读检查，并先说明改动内容、影响范围和验证方式。
- 真实采集、飞书同步、正式文档覆盖、账号备份和远程写入必须分别获得明确授权；其中一项授权不自动覆盖其他操作。
- 用户要求停止任务后，不得自动恢复、续跑或新建采集任务，除非用户再次明确要求。

## 版本与 Git

- 版本检查、版本号、CHANGELOG、提交、标签、拉取、推送和发布统一调用 `version-manager` Skill；版本规则只以该 Skill 的当前配置为准，不在本文件重复定义。
- AMTools、AMDC 和 AMDA 作为同一仓库管理；子项目不单独提交、打标签或发布。
- Git 操作只处理用户指定的文件，保留工作区中已有的无关修改。
- 未提交、未推送或未同步的动作不得在反馈中表述为已经完成。

## AMDC 采集机制

- 多周任务按采集周串行执行，顺序为最新周到最旧周；完成当前周后才能启动下一周。
- 每个采集周由全部已选账号共同处理该周全部品类的国别应用队列，不得改成一个账号负责一个采集周。
- 账号分工、采集周顺序、并发方式和总进度口径属于受保护的核心机制。修改前必须说明方案、影响和取舍，并获得专项确认。
- 普通采集、全新采集及其自动升级逻辑必须保持历史差异识别能力；修改缓存复用、榜单核对或排名比较时，应覆盖对应契约测试。

## AMDC 看板与控件

- 新增下拉选项时复用看板现有 Top 100/Top 1000 的 `sort-menu`、`sort-trigger`、`sort-options` 组件和交互。
- 弹框、确认、错误提示、下拉栏和日期日历等交互优先遵循项目现有实现；不得在没有设计依据时另建一套近似控件。
- 调试和验证默认使用无头浏览器。服务重启必须设置 `AMDC_NO_OPEN=1`，不得自动弹出可见浏览器窗口。
- 需要启动 Microsoft Edge、Google Chrome 或 Chromium 的可见窗口时，先说明用途并获得确认。
- 修改看板后需要重启服务时，先检查采集任务和飞书批量同步是否正在运行；存在运行中任务时，未经确认不得重启。

## AMDC 运行与飞书同步

- 默认使用语法检查、契约测试、健康检查和只读 API 验证修改，不因排障或验证擅自启动真实采集。
- 账号处于限流状态时不得自行发起新任务。
- 与飞书同步规则无关的修改不触发飞书同步。修改同步规则后，未获得真实同步授权时只做本地或隔离验证。
- 批量同步期间锁定相关同步入口，并以全部选中记录为整体计算和展示进度，不得按单个采集周反复归零或提前显示完成。
- 用户提到“AMDC 数据”时，默认指私有配置绑定的 AMDC 飞书电子表格；只有用户明确指定本地文件时，才使用 Excel 或其他数据源。

## AMDA 数据与报告规则

- AMDA 分析必须绑定明确的 `CollectionManifest` 或不可变数据快照。
- 开始日期范围分析前，必须确认截止日期以及纳入、排除的周度工作表；用户已经给出范围时，先回显范围再执行。
- 不改变 AMDA 的报告结构、分析口径、图表契约和 Demo/正式文档确认边界，除非先获得专项确认。
- 固定结构的市场分析更新可以基于已授权数据直接生成具体观点；准备覆盖正式文档时仍需单独确认。
- Demo 只读取正式文档作为结构与视觉基线。自动化和定时任务只能更新 Demo，正式文档在获得覆盖授权前保持只读。
- 更新已有飞书文档时优先使用局部 block 操作，保留未变更图片、画板、表格和引用的原 token；不得为方便而整篇覆盖。
- 正式文档写入前后必须运行项目已有的数据、数值、结构、图表和文档校验；任一检查失败不得视为完成。

## 安全与私有资源

- 不得提交 `amdc-config.json`、账号登录态、令牌、云资源地址、缓存、日志和运行产物。
- 账号备份目标必须是私有仓库，并从本机私有配置读取。
- 项目介绍、数据表格和市场分析文档地址只保存在本机私有配置中，不写入仓库、日志或交付反馈。
- 新建飞书云文档默认保存到“我的文档库”；用户明确指定其他目录或知识库时，以指定位置为准。

## 文档写作

- 生成、改写或协作 AMTools 文档时，使用 `doc-coauthoring` 整理结构与内容，并使用 `deslop` 检查自然表达和 AI 写作痕迹。

### 1. 写作前确认

- 开始新文档或大范围重构前，先提交大纲和材料清单，等待用户确认；用户已经明确给出完整结构并直接要求修改时，可按该结构执行。
- 大纲列明标题层级、章节顺序和各部分重点。材料清单说明材料用途、支持的事实或观点，以及是否只作结构参考。
- 未获得大纲确认且用户没有提供可直接执行的完整结构时，不得开始正文写作或修改成稿。

### 2. 信息与事实

- 只使用用户提供或授权的资料，不自行搜索云文档或扩大资料范围。
- 明确区分事实、参考、推断、计划和已有成果，不把设想或待验证方案写成已实现能力。
- 资料缺失或存在歧义时先询问，不自行补全会影响文章方向的事实。
- 核心观点和最终判断以用户意见为准；工具和 Skill 只辅助整理、分析与验收。

### 3. 范围与修改

- 严格遵守用户确认的主题、框架、标题、内容重点和修改范围。
- 用户修正范围后立即按最新要求执行，不顺带改动其他内容。
- 删除重要内容前列出具体删除项并获得确认。
- 图片、画板和用户手工调整的素材必须原样保留，除非用户明确要求替换。

### 4. 结构与内容

- 章节顺序和篇幅分配服务于用户指定的重点，每个章节承担明确作用。
- 案例、数据、引用和工具知识只用于支持主线，不得挤占正文重点。
- 通用规则与具体案例分开表达，不把单一场景写成普遍要求。
- 流程类文档写清“做什么、检查什么、交付什么”，避免过度展开背景理论。

### 5. 语言表达

- 默认采用客观叙述，避免第一人称和岗位职责宣言；通过动作、判断和交付结果体现职责。
- 语言自然、直接、前后连贯，删除重复、空泛、模板化和明显带有 AI 痕迹的表达。
- 中文引号统一使用全角“”，不得使用英文双引号。
- 成稿中不得出现写作提示、内部讨论、资料处理过程、认知边界或免责声明式说明。
- 每句话都应服务于事实、逻辑、操作、判断或结论。

### 6. 引用与表格

- 引用靠近对应内容并尽量放在段落末尾；控制数量，适合时使用带超链接的 `[1][2]` 编号。
- 不在正文暴露内部资料处理方式或工具知识来源。
- 飞书内部文档的 `<cite type="doc">` 必须使用底层 Docx token，不得使用 Wiki 节点 token；先解析 Wiki URL，再使用返回的 `document_id`。
- 需要保留飞书编辑器粘贴 Wiki URL 后生成的原生 Logo、样式或预览时，保留原生链接对象，不得重写为 `<cite type="doc">`。
- 接口写入的 URL 预览可能降级为普通超链接；新增此类链接前先单条试写，并在浏览器中核验图标、标题和跳转。
- 表头行和标题列水平、垂直居中；其余正文列默认左对齐、垂直居中。
- 同一列没有单元格需要自动换行时，整列水平居中；主动使用 `<br/>` 拆行且每行均能在列宽内单行显示时，不算自动换行，该列仍可居中。
- 同一列只要存在会触发自动换行的单元格，整列左对齐；较长的说明单元格使用左对齐，同一列不得混用对齐方式。
- 表格写入前逐列确定列宽、水平对齐和垂直对齐，并在内容中显式设置，不依赖编辑器默认样式。
- 表格写入后逐表核验表头、标题列、正文列、自动换行和垂直对齐；任一项不符合时先修正并重新核验。

### 7. 全文验收

- 写入后重新阅读全文，检查结构、重点、逻辑、语言、重复、步骤缺口、事实边界和格式一致性。
- 检查是否残留内部说明、未经证实的结论或超出授权范围的内容。
- 飞书文档除 API 回读外还要检查可见渲染；问号图标、错误资源类型、重复引用、表格错位或素材变化均视为未通过。
- 飞书画板内容使用 `lark-cli` 读取和写入；需要核验可见性和交互时使用 Microsoft Edge，不使用 Playwright 代替画板人工渲染检查。
- 使用 Playwright 验收普通页面时保持无头模式；认证状态只保存在仓库忽略的本机目录中，不在 Markdown、日志或反馈中记录 cookie、token 或密码。
- 交付反馈区分已验证结果、待确认事项和未处理内容。
- 写入成功、语法正确、格式无报错或单项测试通过不能替代全文质量验收。
- 文档包含表格时，验收反馈必须说明表头、标题列、正文列、自动换行和垂直对齐的核验结果。
- 只有全文质量与全部表格格式均通过验收后，才能使用“已完成”或同等表述。

## 变更与交付

- 修改共享契约时，同步更新契约校验和 AMDC、AMDA 两端适配说明。
- 测试范围与变更风险相匹配；修改核心采集、同步或报告逻辑时，必须运行对应专项测试。
- 交付反馈只陈述实际完成并验证过的工作，明确区分验证结果、推断和未测试内容。
- 未执行真实采集、飞书同步、正式文档覆盖或端到端验证时，明确说明未执行，不得用局部检查代替。
